// Initial state
import {UIState} from "../../types/state.ts";

export const initialState: UIState = {
    showFilters: true,
    showMetrics: true,
    showNetworkStats: false,
    showDensity: false,
    showDiameter: false,
    sidebarOpen: false
};